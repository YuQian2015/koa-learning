apps:
  - script   : config/auto-config.js
    name     : 'mongodb'
  - script   : app.js
    name     : 'koa'
    instances: 0
    exec_mode: cluster
    watch  : true
    log_type: "json"
    env    :
      NODE_ENV: development
    env_production:
      NODE_ENV: production
