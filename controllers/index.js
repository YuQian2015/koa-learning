const user = require('./user');
const material = require('./material');
const purchase = require('./purchase');
const purchaseOrder = require('./purchaseOrder');


module.exports = {
  user, material, purchase, purchaseOrder
};
